name: Verify Code Quality

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 18.x
        cache: 'npm'

    - name: Verify app structure
      run: |
        echo "✅ Checking app files..."
        [ -f "www/app.html" ] && echo "✅ app.html found" || (echo "❌ app.html missing" && exit 1)
        [ -f "www/index.html" ] && echo "✅ index.html found" || (echo "❌ index.html missing" && exit 1)
        [ -f "config.xml" ] && echo "✅ config.xml found" || (echo "❌ config.xml missing" && exit 1)
        [ -f "package.json" ] && echo "✅ package.json found" || (echo "❌ package.json missing" && exit 1)
        [ -f "www/logo.svg" ] && echo "✅ logo.svg found" || (echo "❌ logo.svg missing" && exit 1)
        echo "✅ All essential files present!"

    - name: Install dependencies
      run: npm ci

    - name: Verify Groq API configuration
      run: |
        echo "✅ Checking Groq API configuration..."
        if grep -q "api.groq.com" www/index.html; then
          echo "✅ Groq API endpoint configured"
        else
          echo "❌ Groq API endpoint missing"
          exit 1
        fi
        
        if grep -q "mixtral-8x7b-32768" www/index.html; then
          echo "✅ Mixtral model configured"
        else
          echo "⚠️ Model configuration might need update"
        fi

    - name: Verify manifest.json
      run: |
        echo "✅ Checking manifest.json..."
        if grep -q "NexusAI" www/manifest.json; then
          echo "✅ Manifest configuration valid"
        else
          echo "❌ Manifest missing NexusAI name"
          exit 1
        fi

    - name: Verify package.json
      run: |
        echo "✅ Checking package.json..."
        npm list cordova 2>/dev/null || echo "⚠️ Cordova not in dependencies (can be global)"

    - name: Build verification summary
      run: |
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo "✅ CODE VERIFICATION PASSED"
        echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
        echo ""
        echo "✅ All source files present"
        echo "✅ Groq API configured"
        echo "✅ Dependencies valid"
        echo "✅ Config files valid"
        echo ""
        echo "📱 To build APK locally:"
        echo "  npm install"
        echo "  cordova platform add android"
        echo "  cordova build android --release"
        echo ""
        echo "🚀 Ready for deployment!"

