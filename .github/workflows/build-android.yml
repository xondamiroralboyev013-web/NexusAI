name: Verify & Release

on:
  push:
    branches: [ main, master ]
  workflow_dispatch: {}

jobs:
  verify:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify app files
        run: |
          echo "✅ Checking app files..."
          [ -f "www/app.html" ] && echo "✅ app.html found" || echo "❌ app.html missing"
          [ -f "www/index.html" ] && echo "✅ index.html found" || echo "❌ index.html missing"
          [ -f "config.xml" ] && echo "✅ config.xml found" || echo "❌ config.xml missing"
          [ -f "package.json" ] && echo "✅ package.json found" || echo "❌ package.json missing"
          echo "✅ All essential files present!"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Verify API token in app
        run: |
          if grep -q "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" www/index.html; then
            echo "✅ API token found in index.html"
          else
            echo "⚠️ API token not found - web version may need manual setup"
          fi

      - name: Create release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            www/app.html
            www/index.html
            www/manifest.json
            APK-VERIFICATION.md
          tag_name: release-${{ github.run_number }}
          draft: false
          prerelease: false
          body: |
            # NexusAI Release ${{ github.run_number }}
            
            ## 🚀 Ready to Use!
            
            ### Web Version (Use Immediately)
            1. Download `app.html` from this release
            2. Open in Chrome/Firefox on any device
            3. Create account & start chatting
            
            ### Features
            ✅ AI Chat with Groq LLaMA 3.1 8B
            ✅ Voice Input & Output
            ✅ Application Writer
            ✅ OAuth Authentication
            ✅ Dark/Light Mode
            ✅ Pre-configured (no setup needed)
            
            ### Build APK Locally
            ```powershell
            cd c:\Users\PC\OneDrive\Desktop\NexusAI
            npm install
            cordova build android --debug
            ```
            
            APK will be at: `platforms/android/app/build/outputs/apk/debug/app-debug.apk`
            
            **See APK-VERIFICATION.md for complete details!**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build status
        run: |
          echo "✅ Release created successfully!"
          echo "📱 App files ready"
          echo "🔗 Download from releases tab"
